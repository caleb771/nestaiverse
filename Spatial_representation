def compute_bounds(location, dimensions):

    x,y,z = location
    w,l,h = dimensions

    return {
        "min_x": x - w/2,
        "max_x": x + w/2,
        "min_y": y - l/2,
        "max_y": y + l/2
    }
